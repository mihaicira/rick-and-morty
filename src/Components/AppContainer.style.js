import styled from "styled-components";

export const AppContainer = styled.div`
    width: 100vw;
    
    #page-not-found{
        margin-left: 50%;
        transform: translateX(-50%);
    }
    `;